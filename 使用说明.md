# Spine Animation AI 使用说明

本文档介绍如何在本机启动和使用这个 Spine AI 换皮工具。

## 1. 这个项目是做什么的

这个项目用于对已有 Spine 工程进行 AI 换皮、局部重绘和重新打包。

它的核心流程是：

```text
打开已有 Spine 工程
  ↓
读取 skeleton / atlas / png
  ↓
用 gpt-image-2 生成新外观
  ↓
用 fal.ai / Bria 去背景、清理透明边缘
  ↓
按原来的 Spine slot 重新切图和打包 atlas
  ↓
导出新的 Spine skin / atlas / png
```

注意：它不是“一张图自动生成 Spine 骨骼动画”的工具。它会复用原 Spine 工程里的骨骼、slot、动画，只替换外观贴图。

适合处理：

- 人物角色换装、换风格
- 动物、怪物、宠物换皮
- 武器、道具、UI 动效资源换材质
- 对已有 Spine 动画做局部修图

不适合直接处理：

- 只有一张普通 PNG、没有 Spine JSON/atlas 的图片
- 从零自动生成骨骼绑定和动画
- 把人类 Spine 工程大幅改成完全不同结构的生物，例如人变成八爪鱼

---

## 2. 项目目录说明

常用目录如下：

```text
spine-animation-ai/
├── start-reskin-app.bat          # Windows 一键启动脚本
├── 使用说明.md                   # 当前文档
├── examples/
│   └── sombrero/                 # 示例 Spine 工程
└── reskin-app/
    └── app/
        ├── backend/              # FastAPI 后端
        ├── frontend/             # React + Vite 前端
        └── .env.example          # 环境变量示例
```

示例工程路径：

```text
D:\Work\Github\spine-animation-ai\examples\sombrero
```

---

## 3. 启动项目

### 方式一：双击 bat 脚本启动

在项目根目录双击：

```text
start-reskin-app.bat
```

脚本会自动执行：

1. 检查后端虚拟环境
2. 安装后端依赖
3. 检查前端依赖
4. 启动后端服务
5. 启动前端服务
6. 自动打开浏览器

启动成功后访问：

```text
前端：http://127.0.0.1:5173
后端：http://127.0.0.1:8765
接口文档：http://127.0.0.1:8765/docs
```

### 方式二：手动启动

后端：

```bat
cd /d D:\Work\Github\spine-animation-ai\reskin-app\app\backend
.venv\Scripts\python.exe -m uvicorn app.backend.server:app --host 127.0.0.1 --port 8765 --app-dir ..\..
```

前端：

```bat
cd /d D:\Work\Github\spine-animation-ai\reskin-app\app\frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

---

## 4. 配置 API Key

项目现在使用：

- `OPENAI_API_KEY`：调用 `gpt-image-2` 生成整角色新皮肤和局部重绘
- `FAL_KEY`：调用 fal.ai / Bria 去背景、清理边缘
- `SAM_SERVER_URL`：可选，用于 SAM 精细分割

### 推荐配置方式：在 Settings 中填写

启动前端后：

1. 打开页面 `http://127.0.0.1:5173`
2. 点击右上角或顶部的 `Settings`
3. 在 `API keys` 区域填写：

```text
OPENAI_API_KEY=你的 OpenAI API Key
FAL_KEY=你的 fal.ai API Key
```

如果暂时不用 SAM，`SAM_SERVER_URL` 可以留空。

Settings 中保存的 key 只保存在本机，位置大致是：

```text
~/.genie-reskin
```

### 也可以使用 .env 文件

复制：

```text
reskin-app/app/.env.example
```

新建：

```text
reskin-app/app/.env
```

填写：

```env
OPENAI_API_KEY=你的 OpenAI API Key
FAL_KEY=你的 fal.ai API Key

# 可选，不填默认使用官方 OpenAI 地址
OPENAI_IMAGE_API_URL=https://api.openai.com/v1

# 可选，只有选择 SAM segmentation 时才需要
SAM_SERVER_URL=
```

---

## 5. Open Project 打开的是什么

`Open Project` 打开的不是代码工程，而是 Spine 资源工程目录。

一个可打开的 Spine 工程目录通常包含：

```text
MyCharacter/
├── skeleton.json 或 Spine.json
├── skeleton.atlas 或 Spine.atlas
└── skeleton.png 或 Spine.png
```

也可能包含拆分后的部件图片：

```text
MyCharacter/
├── skeleton.json
├── skeleton.atlas
├── skeleton.png
├── head.png
├── body.png
├── left_arm.png
└── right_arm.png
```

最小要求：

1. 至少一个 Spine JSON 文件
2. 至少一个 atlas 文件
3. atlas 对应的 PNG 贴图

可以先用示例工程测试：

```text
D:\Work\Github\spine-animation-ai\examples\sombrero
```

---

## 6. 基础使用流程

### 第一步：启动项目

双击：

```text
start-reskin-app.bat
```

浏览器打开：

```text
http://127.0.0.1:5173
```

### 第二步：配置 API Key

点击 `Settings`，填写：

```text
OPENAI_API_KEY
FAL_KEY
```

### 第三步：打开 Spine 工程

点击 `Open Project`。

输入工程目录，例如：

```text
D:\Work\Github\spine-animation-ai\examples\sombrero
```

点击 `Open`。

如果目录里有多个 skeleton / atlas，应用会弹出选择窗口，让你选择要打开哪一个。

### 第四步：预览角色

打开成功后，画布中会显示 Spine 角色。

你可以查看：

- 当前 skin
- slots / parts
- 生成后的新 look
- 局部部件贴图

### 第五步：生成整体换皮

点击 `Generate`。

输入你想要的新风格，例如：

```text
Make this character a cyberpunk robot, keep the same pose and silhouette, clean game asset style.
```

或者中文也可以，例如：

```text
把这个角色改成冰雪法师风格，保持原来的姿势和轮廓，适合 2D 手游角色。
```

点击生成后，流程大致是：

```text
当前 Spine 角色合成图
  ↓
gpt-image-2 根据 prompt 生成新外观
  ↓
fal.ai / Bria 去背景
  ↓
按照原 slot 分割并重新打包
  ↓
生成新的 skin
```

生成完成后，可以在 skin / look 下拉中切换新外观。

---

## 7. Prompt 编写建议

由于项目要把生成图切回原来的 Spine slot，prompt 最好强调保持结构。

推荐写法：

```text
Transform this character into a lava warrior. Keep the same pose, body proportions, silhouette, and readable separated parts. Clean transparent-background 2D mobile game asset.
```

中文示例：

```text
把这个角色改成熔岩战士风格，保持原来的姿势、身体比例、整体轮廓和部件结构，适合 2D 手游，背景干净。
```

建议包含：

- 目标风格：赛博朋克、冰雪、熔岩、机械、Q版等
- 保持姿势：keep same pose
- 保持轮廓：keep same silhouette
- 保持部件结构：keep separated parts
- 游戏资源风格：2D mobile game asset

尽量避免：

- 让角色结构变化太大
- 增加大量原工程没有的翅膀、触手、长裙、披风
- 要求复杂背景
- 让角色转身、换姿势、换视角

---

## 8. 局部 Retouch 使用方法

如果整体生成后只有某个部件不好，可以用局部重绘。

操作方式：

1. 在右侧 parts / slot 列表中选择一个部件
2. 点击 `Retouch`
3. 输入局部修改描述
4. 点击确认

示例：

```text
Make the helmet darker metallic blue, keep the exact silhouette.
```

中文：

```text
把这个头盔改成深蓝色金属材质，保持完全相同的轮廓。
```

Retouch 只会重绘当前 slot 对应的贴图，不会重新生成整个角色。

适合修：

- 某个手臂颜色不对
- 头发边缘不好
- 武器材质不满意
- 衣服局部花纹需要调整

---

## 9. Mask 编辑器使用方法

生成后如果某个 slot 的切图边缘不对，可以进入 Mask 编辑器修正。

常见工具：

- Brush：往 mask 里加区域
- Eraser：从 mask 中擦掉区域
- Lasso：套索选择区域
- Magic：基于 SAM 的智能选择，需要相关模型/服务支持

使用流程：

1. 选择生成后的 skin
2. 选择有问题的 slot
3. 打开 Mask 编辑器
4. 用 Brush / Eraser 修正蓝色 mask 区域
5. 保存
6. 后端重新 rebake atlas

适合处理：

- 部件缺了一块
- 多切进了其他部件
- 边缘透明残留
- slot 分割不准确

---

## 10. Settings 里的重要选项

### API keys

必填：

```text
OPENAI_API_KEY
FAL_KEY
```

可选：

```text
SAM_SERVER_URL
```

### Reskin method

用于选择整体换皮的处理策略。

一般保持默认即可。

### Segmentation method

用于选择如何把生成后的整图切回部件。

常见选择：

```text
bg_components
```

适合先跑通流程，不需要 SAM。

```text
sam
```

需要 `SAM_SERVER_URL`，适合更精细地分割重叠部件。

建议：

- 初次使用：选 `bg_components`
- 部件切割不准：再考虑接 SAM

### Mask erosion

用于收缩 mask 边缘，减少白边、光晕、脏边。

如果发现：

- 边缘有一圈浅色污染
- 部件周围带背景残留

可以适当开启或调大 erosion。

如果发现：

- 细节被吃掉
- 头发丝、尖角变少

可以调小 erosion 或关闭。

### Reference image

可以上传一张风格参考图。

适合：

- 统一多个角色的画风
- 参考某套服装风格
- 参考某种材质或配色

使用时建议同时写清楚：

```text
Use the reference image only for art style and color palette. Keep the source character pose and silhouette.
```

---

## 11. 导出结果

生成满意后，点击 `Export`。

导出通常会包含：

- 新的 Spine JSON
- 新的 atlas
- 新的 PNG 贴图
- 生成后的 skin 资源

导出的结果可以再导入 Spine / 游戏项目中测试。

如果导出后动画显示异常，优先检查：

1. atlas 和 png 是否在同一目录
2. JSON 引用的 attachment 名称是否正确
3. 游戏运行时使用的 Spine 版本是否兼容
4. 是否切换到了新生成的 skin

---

## 12. 常见问题

### 点击 Open Project 报 prompt() is not supported

这个问题已经修复。现在 `Open Project` 会使用应用内弹窗输入路径，而不是浏览器原生 prompt。

如果仍然看到旧错误，请重启前端服务。

### Open Project 打不开目录

检查目录中是否有：

```text
.json
.atlas 或 .atlas.txt
.png
```

并确认路径是本机绝对路径，例如：

```text
D:\Work\Github\spine-animation-ai\examples\sombrero
```

不要填 URL，也不要填单个文件路径。

### Generate 按钮提示缺少 key

需要配置：

```text
OPENAI_API_KEY
FAL_KEY
```

可以在 Settings 中填，也可以写入：

```text
reskin-app/app/.env
```

修改 `.env` 后建议重启后端。

### OpenAI 调用失败

常见原因：

1. `OPENAI_API_KEY` 错误
2. OpenAI 账户没有可用额度
3. 当前网络无法访问 OpenAI
4. `OPENAI_IMAGE_API_URL` 配错
5. 图片尺寸或请求参数被服务拒绝

如果使用代理或兼容网关，可以配置：

```env
OPENAI_IMAGE_API_URL=https://你的网关/v1
```

不要写成：

```text
https://你的网关/v1/images/edits
```

项目会自动拼接 `/images/edits`。

### fal.ai 调用失败

检查：

```text
FAL_KEY
```

fal.ai 主要用于背景移除。如果它失败，通常会导致生成图无法干净切回透明部件。

### 生成结果边缘有白边或脏边

尝试：

1. 在 Settings 中调整 `Mask erosion`
2. 换更明确的 prompt，要求 clean transparent background
3. 用 Mask 编辑器手动擦除脏边
4. 尝试 SAM segmentation

### 生成后部件切错

可能原因：

- prompt 让角色结构变化太大
- 原始 slot 重叠复杂
- 没有使用 SAM 精细分割
- 生成图和原图轮廓差异太大

解决方式：

1. prompt 中强调保持原轮廓和部件结构
2. 在 Settings 中切换分割策略
3. 使用 Mask 编辑器修正
4. 使用 Retouch 单独修某个 slot

### 只想预览 Spine 工程，需要 API Key 吗

不需要。

只打开和预览 Spine 工程不需要 OpenAI 或 fal.ai。

只有使用以下功能时才需要 key：

- Generate 整体换皮
- Retouch 局部重绘
- rebake / 去背景相关流程

---

## 13. 推荐的第一次测试流程

建议按下面流程测试：

1. 双击启动：

```text
start-reskin-app.bat
```

2. 打开浏览器：

```text
http://127.0.0.1:5173
```

3. 点击 `Open Project`

4. 输入示例目录：

```text
D:\Work\Github\spine-animation-ai\examples\sombrero
```

5. 确认角色能正常显示

6. 打开 `Settings`，填写：

```text
OPENAI_API_KEY
FAL_KEY
```

7. Segmentation method 先选择：

```text
bg_components
```

8. 点击 `Generate`

9. 输入 prompt：

```text
Transform this character into an ice wizard, keep the same pose, body proportions, silhouette, and separated parts. Clean 2D mobile game asset, transparent background.
```

10. 生成完成后检查新 skin

11. 如果某个部件不好，用 `Retouch` 修

12. 如果切割边缘不好，用 `Mask` 修

13. 满意后 `Export`

---

## 14. 当前模型分工

当前项目已经改成：

```text
gpt-image-2：负责整图生成和局部重绘
fal.ai / Bria：负责背景移除和透明边缘清理
SAM：可选，负责更精细的部件分割
```

也就是说：

```text
Gemini 已不再作为主要生成模型使用。
```

你现在主要需要准备：

```text
OPENAI_API_KEY
FAL_KEY
```

---

## 15. 使用建议

为了得到更稳定的 Spine 换皮结果：

1. 尽量选择结构清晰的 Spine 工程
2. prompt 中明确保持姿势、轮廓和部件结构
3. 不要要求生成复杂背景
4. 不要让角色结构变化太大
5. 先用 `bg_components` 跑通，再考虑 SAM
6. 整体生成后，用 Retouch 修小问题
7. 切割问题用 Mask 编辑器修
8. 导出后一定在 Spine 或游戏运行时里测试动画
